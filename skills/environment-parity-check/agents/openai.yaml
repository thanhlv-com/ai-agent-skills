interface:
  display_name: "Environment Parity Check"
  short_description: "Reduce dev/staging/prod environment drift."
  default_prompt: "Use this skill to align environment settings and deployment assumptions, apply focused changes, run relevant validations, and summarize decisions and risks."
