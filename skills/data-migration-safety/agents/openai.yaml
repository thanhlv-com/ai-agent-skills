interface:
  display_name: "Data Migration Safety"
  short_description: "Design reversible and safe schema/data migrations."
  default_prompt: "Use this skill to create forward/backward-safe migrations and validation checks, apply focused changes, run relevant validations, and summarize decisions and risks."
