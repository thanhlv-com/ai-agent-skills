interface:
  display_name: "Naming Consistency Cleanup"
  short_description: "Standardize naming across code and APIs."
  default_prompt: "Use this skill to normalize naming conventions and remove ambiguity, apply focused changes, run relevant validations, and summarize decisions and risks."
