interface:
  display_name: "Database Index Strategy"
  short_description: "Design and tune database indexing effectively."
  default_prompt: "Use this skill to optimize index usage based on real query patterns, apply focused changes, run relevant validations, and summarize decisions and risks."
