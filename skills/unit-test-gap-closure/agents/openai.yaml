interface:
  display_name: "Unit Test Gap Closure"
  short_description: "Add unit coverage for uncovered logic branches."
  default_prompt: "Use this skill to close unit-level coverage gaps for changed behavior, apply focused changes, run relevant validations, and summarize decisions and risks."
