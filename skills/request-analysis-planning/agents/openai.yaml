interface:
  display_name: "Request Analysis Planning"
  short_description: "Analyze and plan before implementation on every request."
  default_prompt: "For every request, first analyze scope, assumptions, dependencies, and risks; then produce an ordered execution plan (compact for simple tasks, detailed for non-trivial work) with validation gates before implementing and reporting outcomes."
