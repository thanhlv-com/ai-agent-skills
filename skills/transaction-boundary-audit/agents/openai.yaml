interface:
  display_name: "Transaction Boundary Audit"
  short_description: "Validate transaction scope and atomicity guarantees."
  default_prompt: "Use this skill to audit transaction boundaries and fix partial-write risks, apply focused changes, run relevant validations, and summarize decisions and risks."
