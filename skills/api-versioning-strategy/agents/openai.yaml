interface:
  display_name: "API Versioning Strategy"
  short_description: "Manage API version rollout and backward compatibility."
  default_prompt: "Use this skill to plan and enforce safe API versioning and deprecation steps, apply focused changes, run relevant validations, and summarize decisions and risks."
