interface:
  display_name: "Config Sprawl Reduction"
  short_description: "Consolidate and validate fragmented configuration."
  default_prompt: "Use this skill to reduce config duplication and clarify override precedence, apply focused changes, run relevant validations, and summarize decisions and risks."
