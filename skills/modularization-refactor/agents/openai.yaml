interface:
  display_name: "Modularization Refactor"
  short_description: "Split oversized modules into cohesive units."
  default_prompt: "Use this skill to refactor code into smaller modules with clear responsibilities, apply focused changes, run relevant validations, and summarize decisions and risks."
